<h1>タスクリスト一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>タスク<%= link_to task.id, task %> :<% task.status %> &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの追加', new_task_path %>